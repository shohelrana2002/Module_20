function tenTimes(number) {
  const result = number * 10;
  return result;
}
// return a var er nam return korte habe
function cutHalf(number) {
  const half = number / 2;
  return half;
}

const cutHalfResult = cutHalf(506);
console.log(cutHalfResult);

const tenTimesResult = tenTimes(5);
console.log(tenTimesResult);
